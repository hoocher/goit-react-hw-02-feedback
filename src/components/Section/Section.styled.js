import styled from 'styled-components';

export const SectionSec = styled('section')(() => {
  return {
    width: '50vw',
    height: '170px',
    margin: '0 auto',
    marginTop: '20px',
    marginBottom: ' 20px',
    backgroundColor: '#D9D9D9',
    padding: '20px',
    textAlign: 'center',
  };
});
